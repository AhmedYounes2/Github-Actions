name: Events filters

on:
  pull_request:
    types:
      - closed
      - reopened


jobs:
  print-event-filters:
    runs-on: ubuntu-latest
    steps:
      - name: print Event filters
        run: echo "Events filters basics"
