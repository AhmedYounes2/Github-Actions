name: Events filters

on:
  push:
    branches:
      - main


jobs:
  print-event-filters:
    runs-on: ubuntu-latest
    steps:
      - name: print Event filters
        run: echo "Events filters basics"
